import { ImageAuthPipe } from './image-auth.pipe';

describe('ImageAuthPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageAuthPipe();
    expect(pipe).toBeTruthy();
  });
});
