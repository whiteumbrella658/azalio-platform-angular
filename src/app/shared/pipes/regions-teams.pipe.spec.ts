import { RegionsTeamsPipe } from './regions-teams.pipe';

describe('RegionsTeamsPipe', () => {
  it('create an instance', () => {
    const pipe = new RegionsTeamsPipe();
    expect(pipe).toBeTruthy();
  });
});
